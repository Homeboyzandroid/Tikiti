<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="fixxit.homeboyz.tikiti">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".Adapters.AppController"
        android:allowBackup="true"
        android:icon="@mipmap/tikiti"
        android:label="@string/app_name"
        android:supportsRtl="true"
        tools:replace="android:icon"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivityDrawer"
            android:label="@string/Tikiti"
            android:theme="@style/AppTheme.NoActionBar">

        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".EventDetails" />
        <activity android:name=".SplashActivity"
            android:theme="@style/AppTheme.NoActionBar">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>

        <activity android:name=".EventSubcategory" />
        <activity android:name=".GetTicket"></activity>
    </application>

</manifest>
